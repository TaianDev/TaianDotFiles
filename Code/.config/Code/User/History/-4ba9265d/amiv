-- Modules
--require("lua_modules.animations")
--require("lua_modules.autostart")
--require("lua_modules.env")
--require("lua_modules.input")
--require("lua_modules.keybinds")
--require("lua_modules.monitors")
-- require("lua_modules.plugins") -- No plugins for now
--require("lua_modules.autostart")

-- General config
hl.config({
    general = {
        gaps_in         = 5,
        gaps_out        = 9,
        border_size     = 1,
        col = {
            active_border   = "rgba(FFFFFF40)",
            inactive_border = "rgba(595959aa)"
        },
        resize_on_border = false,
        allow_tearing    = false,
		-- General layout (can be overridden per workspace with dynamic rules controlled by keybinds)
        layout           = "dwindle",
    },
	-- Decoration
    decoration = {
        rounding        = 4,
        rounding_power  = 2,
		-- Opacity and shadows
        active_opacity  = 1.0,
		inactive_opacity = 0.8,
        shadow = {
            enabled      = true,
            range        = 20,
            render_power = 15,
            color        = "rgba(0, 0, 0, 0.5)",
        },
		-- Blur options
        blur = {
            enabled           = true,
            new_optimizations = true,
            size              = 4,
            passes            = 4,
            contrast          = 1,
            vibrancy          = 0.2,
            vibrancy_darkness = 0.2,
            ignore_opacity    = true,
            popups            = true,
        },
		-- On/off animations
		animations = {
        	enabled = true,
    	}
    },

-- PROGRES
    scrolling = {
        fullscreen_on_one_column = true,
        column_width             = 0.8,
        focus_fit_method         = 1,
        direction                = "down",
    },
    master = {
        new_status = "master",
    },
    misc = {
        force_default_wallpaper = -1,
        disable_hyprland_logo   = true,
    },
})