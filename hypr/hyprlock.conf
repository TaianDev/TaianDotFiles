##########################################
##########################################
########                       ###########
######## THAT IS NOT MY CONFIG ###########
########                       ###########
##########################################
##########################################
########                        ##########
########   CONFIG BY: ASHRREAL  ##########
########                        ##########
##########################################
##########################################


# ==> Can found the original source on: https://github.com/ASHRREAL/hypr


#--------------------------------------------------------------------------------#
#--------------------------------------------------------------------------------#

#source = $HOME/.config/hypr/macchiato.conf

$accent = $mauve
$accentAlpha = $mauveAlpha
$font = CaskaydiaCove Nerd Font

# ==============================================================================
# GENERAL SETTINGS
# ==============================================================================

general {
    hide_cursor = true
    grace = 0
    disable_loading_bar = false
    no_fade_in = false
}

# ==============================================================================
# BACKGROUND
# ==============================================================================

background {
    monitor =
#   path = /home/taian/Descargas/backiee-314716-landscape.jpg
    path = /home/taian/Imágenes/Fondos/lockscreen.png
    blur_passes = 4
    blur_size = 2
    noise = 0.0117
    contrast = 1.3000
    brightness = 0.8000
    vibrancy = 0.2100
    vibrancy_darkness = 0.1
    color = $base
}

# ==============================================================================
# FLOATING PARTICLES
# ==============================================================================

# Particle 1
label {
    monitor =
    text = ✦
    color = rgba(255, 255, 255, 0.4)
    font_size = 12
    font_family = $font
    position = -80, 200
    halign = center
    valign = center
}

# Particle 2
label {
    monitor =
    text = ✧
    color = rgba(255, 255, 255, 0.6)
    font_size = 8
    font_family = $font
    position = 120, -150
    halign = center
    valign = center
}

# Particle 3
label {
    monitor =
    text = ✨
    color = rgba(255, 255, 255, 0.5)
    font_size = 10
    font_family = $font
    position = -200, -100
    halign = center
    valign = center
}

# Particle 4
label {
    monitor =
    text = ⭐
    color = rgba(255, 255, 255, 0.4)
    font_size = 6
    font_family = $font
    position = 180, 150
    halign = center
    valign = center
}

# Particle 5
label {
    monitor =
    text = ◆
    color = rgba(255, 255, 255, 0.3)
    font_size = 14
    font_family = $font
    position = -150, 50
    halign = center
    valign = center
}

# ==============================================================================
# DECORATIVE BORDERS
# ==============================================================================

label {
    monitor =
    text = ╭─────────────────────────────────────────╮
    color = rgba(255, 255, 255, 1)
    font_size = 16
    font_family = JetBrains Mono
    position = 0, 280
    halign = center
    valign = center
}

label {
    monitor =
    text = ╰─────────────────────────────────────────╯
    color = rgba(255, 255, 255, 1)
    font_size = 16
    font_family = JetBrains Mono
    position = 0, -280
    halign = center
    valign = center
}

# ==============================================================================
# TIME
# ==============================================================================

# Time glow effect (behind main time)
label {
    monitor =
    text = $TIME
    color = rgba(255, 255, 255, 0.2)
    font_size = 85
    font_family = $font
    position = 2, 152
    halign = center
    valign = center
}

# Main time display
label {
    monitor =
    text = $TIME
    color = $text
    font_size = 80
    font_family = $font
    position = 0, 150
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 8
    shadow_color = rgba(203, 166, 247, 0.6)
}

# ==============================================================================
# DATE - Below time
# ==============================================================================

label {
    monitor =
    text = cmd[update:3600000] date +"%A, %d %B %Y"
    color = $subtext1
    font_size = 20
    font_family = $font
    position = 0, 80
    halign = center
    valign = center
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(0, 0, 0, 0.6)
}

# ==============================================================================
# UPTIME
# ==============================================================================

label {
    monitor =
    text = cmd[update:60000] uptime -p | sed 's/up /⏱️ /'
    color = $green
    font_size = 12
    font_family = $font
    position = 30, -35
    halign = left
    valign = top
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(166, 227, 161, 0.4)
}

# ==============================================================================
# SYSTEM STATUS INDICATORS
# ==============================================================================

# CPU Usage
label {
    monitor =
    text = cmd[update:2000] echo "CPU: $(top -bn2 -d0.1 | grep "Cpu(s)" | tail -1 | awk '{printf "%.1f%%\n", 100-$8}') | Uptime: $(uptime | tail -c 17)"
    color = $peach
    font_size = 12
    font_family = $font
    position = -30, -35
    halign = right
    valign = top
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(250, 179, 135, 0.3)
}

# RAM Usage
label {
    monitor =
    text = cmd[update:3000] free | grep Mem | awk '{printf "RAM: %.0f%%", $3/$2 * 100.0}'
    color = $yellow
    font_size = 12
    font_family = $font
    position = -30, -55
    halign = right
    valign = top
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(250, 227, 176, 0.3)
}

# ==============================================================================
# DECORATION
# ==============================================================================

# Diamond top left
label {
    monitor =
    text = ◇
    color = rgba(255, 255, 255, 0.4)
    font_size = 30
    font_family = $font
    position = -200, 200
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 6
    shadow_color = rgba(203, 166, 247, 0.2)
}

# Triangle top right
label {
    monitor =
    text = △
    color = rgba(255, 255, 255, 0.5)
    font_size = 25
    font_family = $font
    position = 200, 200
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 5
    shadow_color = rgba(245, 194, 231, 0.2)
}

# Hexagon bottom left
label {
    monitor =
    text = ⬢
    color = rgba(255, 255, 255, 0.4)
    font_size = 35
    font_family = $font
    position = -200, -250
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 7
    shadow_color = rgba(139, 213, 202, 0.2)
}

# Star bottom right
label {
    monitor =
    text = ✦
    color = rgba(255, 255, 255, 0.6)
    font_size = 28
    font_family = $font
    position = 200, -250
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 6
    shadow_color = rgba(250, 227, 176, 0.2)
}

# ==============================================================================
# YOU KNOW
# ==============================================================================

label {
    monitor =
    text = cmd[update:8000] printf '%s\n' '⚖️ La justicia consiste en dar a cada uno lo que le corresponde — Aristóteles' '🕊 El hombre nace libre, pero en todas partes está encadenado — Rousseau' '🏛 El hombre es un animal político, destinado a vivir en comunidad — Aristóteles' '🔓 La libertad de uno termina donde comienza la del otro — John Stuart Mill' '✍️ La paz perpetua no es natural, sino un ideal que debe alcanzarse — Kant' '⏳ El hombre está condenado a ser libre, sostuvo el existencialismo — Sartre' '⚔️ La igualdad de derechos es la base de la libertad política — Declaración de 1789' '⭐ La ética se funda en el respeto a la dignidad de toda persona — Kant' '🛡 La vida en el estado natural es solitaria, pobre y brutal — Hobbes' '📚 La virtud es un hábito que nos conduce al justo medio — Aristóteles' '✋ La moral es el conjunto de reglas que hacen posible la convivencia — Durkheim' '⚖️ La justicia es la primera virtud de las instituciones sociales — John Rawls' '🕊 La libertad consiste en poder hacer todo lo que no daña a otro — Declaración de 1789' '🏛 La libertad política es la participación en el poder de la comunidad — Aristóteles' '🔓 La libertad es obedecer la ley que uno mismo se ha impuesto — Rousseau' '✍️ El contrato social es el fundamento legítimo de la autoridad política — Rousseau' '⏳ El hombre no es nada más que lo que hace de sí mismo — Sartre' '⚔️ La vida sin examen no merece ser vivida — Sócrates' '⭐ El hombre es un fin en sí mismo, nunca un simple medio — Kant' '🛡 El Estado es necesario para evitar que el hombre sea un lobo para el hombre — Hobbes' '📚 El conocimiento es poder, fundamento de toda acción política — Francis Bacon' '✋ La educación es el arma más poderosa para cambiar el mundo — Nelson Mandela' '⚖️ Donde termina la libertad, comienza la tiranía — John Locke' '🕊 La religión es el opio del pueblo — Karl Marx' '🏛 La historia de la humanidad es la historia de la lucha de clases — Marx' '🔓 Las acciones son correctas en la medida en que promueven la felicidad — John Stuart Mill' '✍️ La moral no se deriva de la razón, sino de los sentimientos — David Hume' '⏳ No se puede entrar dos veces en el mismo río, todo fluye — Heráclito' '⚔️ Es mejor ser temido que amado, si no se puede ser ambas cosas — Maquiavelo' '⭐ El fin supremo de la vida humana es la felicidad — Aristóteles' | shuf -n1
    color = $pink
    font_size = 16
    font_family = $font
    position = 0, -200
    halign = center
    valign = center
    text_align = center
    shadow_passes = 3
    shadow_size = 6
    shadow_color = rgba(245, 194, 231, 0.4)
}

# ==============================================================================
# USER AVATAR
# ==============================================================================

# Avatar outer glow
image {
    monitor =
    path = /home/taian/Imágenes/Fondos/Avatares/gato.jpg
    size = 95
    border_size = 1
    border_color = rgba(203, 166, 247, 0.2)
    position = 0, -8
    halign = center
    valign = center
    shadow_passes = 4
    shadow_size = 15
    shadow_color = rgba(203, 166, 247, 0.15)
}

# Main avatar
image {
    monitor =
    path = /home/taian/Imágenes/Fondos/Avatares/gato.jpg
    size = 80
    border_size = 3
    border_color = rgba(255, 255, 255, 0.5)
    position = 0, -10
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 8
    shadow_color = rgba(139, 125, 255, 0.5)
}

# ==============================================================================
# WELCOME MESSAGE - Below avatar
# ==============================================================================

label {
    monitor =
    text = Bienvenido de nuevo, $USER
    color = $text
    font_size = 18
    font_family = $font
    position = 0, -80
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(193, 245, 199, 0.3)
}

# ==============================================================================
# INPUT FIELD - Below welcome message
# ==============================================================================

input-field {
    monitor =
    size = 280, 45
    outline_thickness = 5
    dots_size = 0.25
    dots_spacing = 0.3
    dots_center = true
    dots_rounding = -1
    outer_color = rgba(100, 81, 81, 0.2)
    inner_color = $surface0
    font_color = $text
    fade_on_empty = false
    fade_timeout = 1000
    placeholder_text = CONTRASEÑA? 😴
    hide_input = false
    check_color = rgba(39, 245, 58, 0.60)
    fail_color = rgba(245, 39, 39, 0.60)
    fail_text = NOUUUUUU! ($ATTEMPTS)
    capslock_color = $yellow
    position = 0, -140
    halign = center
    valign = center
    shadow_passes = 4
    shadow_size = 10
    shadow_color = rgba(255, 255, 255, 0.3)
}

# ==============================================================================
# CORNER ACCENTS
# ==============================================================================

# Top left corner
label {
    monitor =
    text = ╭─
    color = rgba(255, 255, 255, 1)
    font_size = 20
    font_family = JetBrains Mono
    position = 15, -15
    halign = left
    valign = top
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# Top right corner
label {
    monitor =
    text = ─╮
    color = rgba(255, 255, 255, 1)
    font_size = 20
    font_family = JetBrains Mono
    position = -15, -15
    halign = right
    valign = top
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# Bottom left corner
label {
    monitor =
    text = ╰─
    color = rgba(255, 255, 255, 1)
    font_size = 20
    font_family = JetBrains Mono
    position = 15, 15
    halign = left
    valign = bottom
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# Bottom right corner
label {
    monitor =
    text = ─╯
    color = rgba(255, 255, 255, 1)
    font_size = 20
    font_family = JetBrains Mono
    position = -15, 15
    halign = right
    valign = bottom
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}


# ==============================================================================
# AMBIENT GLOW ELEMENTS
# ==============================================================================

# Central glow
label {
    monitor =
    text = ●
    color = rgba(193, 245, 199, 0)
    font_size = 400
    font_family = $font
    position = 0, 0
    halign = center
    valign = center
}

# Side glows
label {
    monitor =
    text = ●
    color = rgba(193, 245, 199, 0.03)
    font_size = 200
    font_family = $font
    position = -300, 0
    halign = center
    valign = center
}

label {
    monitor =
    text = ●
    color = rgba(193, 245, 199, 0.03)
    font_size = 200
    font_family = $font
    position = 300, 0
    halign = center
    valign = center
}

# ==============================================================================
# BOTTOM INFO
# ==============================================================================

# Battery - bottom left
label {
    monitor =
    text = cmd[update:60000] /home/taian/Personal_Scripts/Hyprlock/battey_o.sh 
    color = $green
    font_size = 14
    font_family = $font
    position = 30, 35
    halign = left
    valign = bottom
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(166, 227, 161, 0.4)
}

# Network status - bottom right
label {
    monitor =
    text = cmd[update:5000] timeout 1s bash -c 'if ping -c 1 -W 1 8.8.8.8 &>/dev/null; then echo "● Online"; else echo "○ Offline"; fi'
    color = $blue
    font_size = 12
    font_family = $font
    position = -30, 35
    halign = right
    valign = bottom
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(137, 220, 235, 0.4)
}
